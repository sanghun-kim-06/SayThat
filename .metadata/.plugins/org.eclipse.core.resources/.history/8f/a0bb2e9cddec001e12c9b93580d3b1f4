package dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import common.DBUtils;


public class SayThatDao {
	public String userIdCheck(HttpServletRequest req, HttpServletResponse resp) {
		Connection conn = null;
		PreparedStatement psmt = null;
		ResultSet rs = null;
		String result = "";
		
		try {
			
			conn = DBUtils.getConnection();
			String id = req.
			String sql = " SELECT userid FROM USERS where userid = 2 ";
			
			psmt = conn.prepareStatement(sql);
			System.out.println("sql 실행 준비");
			rs = psmt.executeQuery();
			System.out.println("디비 실행 ");
			if(rs.next()) {
				userId = rs.getString("1");
				result = "사용 가능한 ID입니다.";
			}
		} catch (Exception e) {
			e.printStackTrace();
		}finally {
			DBUtils.close(conn, psmt, rs);
		}
		
		return result;
		

	}

}
